server:
  port: 9002

spring:
  h2:
    console:
      enabled: true
  application:
    name: PAYMENT-SERVICE

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance: localhost



